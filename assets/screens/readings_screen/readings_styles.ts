import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'flex-start',
    paddingTop: '10%',
  },
  title: {
    fontSize: 40,
    fontWeight: 'bold',
    width: '100%',
    textAlign: 'left',
    marginVertical: 10,
    paddingHorizontal: '5%',
  },
  scrollView: {
    width: '100%',
    gap: 10,
    paddingBottom: 200,
  },
});
